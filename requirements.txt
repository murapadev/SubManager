# Async HTTP client
# Lower bounds chosen so prebuilt wheels exist for Python 3.13 (no gcc needed).
aiohttp>=3.10.11
aiofiles>=23.2.1

# Core dependencies
certifi>=2024.12.14
charset-normalizer>=3.4.1
idna>=3.10

# YAML configuration
PyYAML>=6.0.2

# Optional: for better console output
tqdm>=4.66.1
colorama>=0.4.6

# For type hints (optional but recommended)
typing-extensions>=4.9.0
